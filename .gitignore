# build output
dist/
# book / ebook / cover products — paid editions, generated locally into
# artifacts/ (outside the dist/ publish root); never committed, never published
artifacts/
# generated types
.astro/

# hardware build repos — each is its own standalone git repo
# (e.g. builds/daily-driver -> github.com/makerphones/daily-driver),
# tracked independently, not vendored into the website repo.
builds/

# dependencies
node_modules/

# logs
npm-debug.log*

# environment variables
.env
.env.production

# macOS
.DS_Store

# editor / harness local config
.claude/
